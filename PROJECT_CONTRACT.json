{
  "schema_version": 1,
  "metrics": {
    "production_loc": 3366,
    "production_files": 22,
    "production_packages": 8,
    "test_files": 9,
    "production_functions": 62,
    "decision_points": 173
  },
  "persistence": {
    "module": "go.etcd.io/bbolt",
    "entities": [
      "CaseMatter",
      "Document",
      "DocumentVersion",
      "AuditEntry"
    ],
    "reopen_test": "TestPersistenceSurvivesReopen"
  },
  "workflows": [
    {
      "steps": [
        "create a case matter",
        "authorize the partner",
        "add a lawyer member",
        "create the evidence folder"
      ],
      "test": "TestWorkflowCaseSetup"
    },
    {
      "steps": [
        "upload the initial document version",
        "open the document editing context",
        "save a new immutable version",
        "inspect the resulting audit trail"
      ],
      "test": "TestWorkflowUploadEditPreview"
    },
    {
      "steps": [
        "create a controlled share link",
        "validate its expiry and download scope",
        "download the current document version",
        "record the external access event"
      ],
      "test": "TestWorkflowShareDownload"
    }
  ]
}
